# For CUDA (GPU) support, install torch from the PyTorch CUDA index instead
# of plain pip, e.g. for CUDA 12.4:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# then install the rest of this file normally. Check https://pytorch.org/get-started/locally/
# for the index URL matching your installed CUDA driver version.
torch>=2.1.0
torch-geometric>=2.4.0
pyyaml
numpy
pytest
tensorboard
